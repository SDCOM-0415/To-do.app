# Todo App v0.3.1 - Python 3.13 依赖包
# 适用于 Python 3.13.5+

# 注意：Python 3.13 需要额外安装 python-tk
# 安装命令：brew install python-tk@3.13

# 可选的现代化 UI 框架（如果系统支持）
customtkinter>=5.2.2

# 图像处理（用于图标等）
Pillow>=10.0.0

# 注意事项：
# 1. tkinter 是 Python 标准库的一部分，但在某些系统上需要单独安装
# 2. 如果 customtkinter 不兼容，程序会自动回退到标准 tkinter
# 3. Python 3.13 在 macOS 上需要通过 Homebrew 安装 python-tk 包