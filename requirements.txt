# Todo App v0.3.1 - Python 依赖包
# 适用于 Python 3.12+ 和 Python 3.13+

# UI 框架
customtkinter>=5.2.2

# 图像处理（用于图标等）
Pillow>=10.0.0

# 注意事项：
# 1. tkinter 是 Python 标准库的一部分，但在某些系统上需要单独安装
# 2. Windows: 安装Python时请确保勾选"tcl/tk and IDLE"选项
# 3. macOS: 可能需要额外安装 python-tk，命令: brew install python-tk@3.12
# 4. Linux: 需要安装 python3-tk，命令: sudo apt-get install python3-tk
